abstract class AppStyles  {
  /// Valor constante para el radio de los bordes redondeados.
  /// Este valor se utiliza en widgets como contenedores, tarjetas, etc.
  static const borderRadius = 15.0;

  /// Valor constante para el padding (relleno) en widgets.
  /// Este valor se utiliza para mantener un espaciado consistente en toda la aplicación.
  static const padding = 10.0;

  /// Valor constante para la altura de los botones.
  /// Este valor se utiliza para mantener un tamaño uniforme en los botones de la aplicación.
  static const heightBotton = 50.0;
  static const iconSize = 25.0;
}
